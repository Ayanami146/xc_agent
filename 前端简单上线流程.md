# 前端简单上线流程（Windows 练习环境）

这份流程只解决一件事：把 Vue 前端放到 Windows 本机的 Nginx 上运行，并把 `/api` 转发到本机后端。访问地址默认是：

```text
http://localhost:8088
```

环境关系：

```text
浏览器
  ↓ http://localhost:8088
Windows Nginx（静态前端）
  ├─ /、/chat、/tickets → frontend/dist
  └─ /api/*             → http://127.0.0.1:8080
                                  ↓
                            你的后端服务
                                  ↓
                         Redis 虚拟机等后端依赖
```

Redis 地址只配置在后端项目中，前端和 Nginx 不需要知道 Redis 的地址。

## 一、先用 mock 把前端上线

这个阶段不需要后端，适合先确认 Node、Vue 和 Nginx 都能正常工作。

打开 PowerShell：

```powershell
cd C:\work_learn\XinChuang_pc\frontend
npm ci
npm run build -- --mode mock
```

构建完成后应生成：

```text
C:\work_learn\XinChuang_pc\frontend\dist\index.html
C:\work_learn\XinChuang_pc\frontend\dist\assets\...
```

mock 模式可以使用页面预填的账号密码登录，不会请求真实后端。

## 二、准备 Windows Nginx

1. 下载 Windows 版 Nginx ZIP。
2. 解压到一个简单目录，例如：

```text
C:\tools\nginx
```

3. 备份原配置：

```powershell
Copy-Item C:\tools\nginx\conf\nginx.conf C:\tools\nginx\conf\nginx.conf.bak
```

4. 用仓库里的配置替换 Nginx 配置：

```powershell
Copy-Item -Force C:\work_learn\XinChuang_pc\deploy\nginx\windows-nginx.conf C:\tools\nginx\conf\nginx.conf
```

配置已经使用当前项目路径，并约定：

- Nginx 前端端口：`8088`
- 后端端口：`8080`
- 前端构建目录：`C:/work_learn/XinChuang_pc/frontend/dist`

如果你的后端不是 `8080`，只需修改配置中的：

```nginx
server 127.0.0.1:8080;
```

## 三、启动 Nginx

打开 PowerShell：

```powershell
cd C:\tools\nginx
.\nginx.exe -t
Start-Process .\nginx.exe -WindowStyle Hidden
```

`nginx.exe -t` 必须显示配置成功后再启动。

浏览器访问：

```text
http://localhost:8088
```

再直接访问并刷新以下地址，均不应出现 Nginx 404：

```text
http://localhost:8088/login
http://localhost:8088/chat
http://localhost:8088/tickets
```

到这里，前端已经以 mock 模式简单上线。

## 四、开始练习后端接口

你的后端在 Windows 本机监听：

```text
http://127.0.0.1:8080
```

开发阶段可以先启动 Vite，获得更方便的热更新：

```powershell
cd C:\work_learn\XinChuang_pc\frontend
Copy-Item .env.example .env.local
npm run dev
```

`.env.local` 中保持：

```dotenv
VITE_API_MODE=remote
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://127.0.0.1:8080
```

此时 Vite 会把 `/api` 转发到你的后端。你可以按照下面的顺序练习，不需要一次写完所有接口：

1. `POST /api/v1/auth/login`：先打通登录。
2. `POST /api/v1/auth/refresh` 和 `/logout`：练习 token/cookie。
3. `GET /api/v1/sessions` 和消息列表：练习数据库分页。
4. `POST /api/v1/chat/stream`：最后练习 SSE。
5. FAQ、手册和工单接口按需逐步实现。

详细字段直接查看：

- `接口文档/智能客服系统接口文档.md`
- `接口文档/openapi.yaml`
- `database/mysql/README.md`（MySQL 建表、开发数据和接口对应关系）

本地使用的是 `http://localhost`，后端开发环境的 refresh Cookie 建议配置为：

```text
HttpOnly=true
SameSite=Lax
Secure=false
Path=/api/v1/auth
```

正式 HTTPS 环境再改为 `Secure=true` 和更严格的 SameSite 策略。不要为了本地调试删除 `HttpOnly`。前端通过 Nginx/Vite 同源访问 API，因此不需要开放 CORS。

## 五、切换为真实后端模式

当需要让 Nginx 上的前端调用你的真实后端时，重新正常构建，不要带 `--mode mock`：

```powershell
cd C:\work_learn\XinChuang_pc\frontend
npm run build
```

Nginx 直接读取 `dist`，构建后通常不必重启。为了确保配置重新加载，可以执行：

```powershell
cd C:\tools\nginx
.\nginx.exe -t
.\nginx.exe -s reload
```

打开浏览器开发者工具，在 Network 中确认请求地址类似：

```text
http://localhost:8088/api/v1/auth/login
```

Nginx 会把它转发为：

```text
http://127.0.0.1:8080/api/v1/auth/login
```

因此前端不需要配置 CORS，也不要在前端代码里直接写 `http://127.0.0.1:8080`。

## 六、日常修改流程

前端代码发生修改后：

```powershell
cd C:\work_learn\XinChuang_pc\frontend
npm run build
```

后端代码发生修改时，只需重启后端；前端通常不必重新构建，除非接口路径或前端代码也变了。

Nginx 配置发生修改后：

```powershell
cd C:\tools\nginx
.\nginx.exe -t
.\nginx.exe -s reload
```

停止 Nginx：

```powershell
cd C:\tools\nginx
.\nginx.exe -s quit
```

## 七、局域网访问（可选）

如果只在本机练习，不需要开放防火墙。

如果希望同一局域网的其他电脑访问：

1. 查询 Windows 本机 IPv4 地址，例如 `192.168.1.20`。
2. 以管理员身份只放行 Nginx 的 `8088` 端口。
3. 其他电脑访问 `http://192.168.1.20:8088`。

不要把后端 `8080`、Redis `6379` 或数据库端口直接开放到公网。

## 八、常见问题

### 页面能打开，但登录提示网络错误

如果当前构建是 remote 模式，这是后端 `8080` 未启动或登录接口尚未实现造成的。可以先重新执行：

```powershell
npm run build -- --mode mock
```

### 刷新 `/chat` 显示 404

确认 Nginx 配置包含：

```nginx
try_files $uri $uri/ /index.html;
```

### 出现 502 Bad Gateway

说明 Nginx 已收到 `/api` 请求，但无法连接 `127.0.0.1:8080`。检查后端是否启动、端口是否一致。

### 修改前端后页面没变化

重新执行 `npm run build`，然后按 `Ctrl+F5` 强制刷新。不要直接部署仓库中以前生成的旧 `dist`。

### SSE 回答一次性显示

确认 `/api/v1/chat/stream` 使用单独的 Nginx location，并包含 `proxy_buffering off`。仓库提供的 Windows 配置已经处理。
